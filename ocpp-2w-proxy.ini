[logging]
proxy = DEBUG
websockets.client = INFO
websockets.server = INFO
; Optional raw websocket payload log file. Leave empty to disable.
;websocket_data_file = websocket-data.log

[host]
; Host to listen on (default: 0.0.0.0)
addr = 0.0.0.0
; Port to listen on (default: 8321)
port = 8321
; watchdog stale time for charger (and server if LC/proxy) connections in seconds.
watchdog_stale = 500
; watchdog run / check interval in seconds
watchdog_interval = 100
; ping timeout in seconds
ping_timeout = 60

[ext-server]
; Comma-separated CSMS server URLs. The first server is primary.
servers = ws://ocpp.it34.com:80, ws://192.168.188.176:8888
; Backward-compatible alternative:
; server = ws://ocpp.it34.com:80
; secondary_server = ws://192.168.188.176:8888
